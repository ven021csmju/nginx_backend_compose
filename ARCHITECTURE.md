# โครงสร้างระบบ Full Stack Application
**Express + Prisma 7 + Supabase + Quasar/Vue**

---

## 🏗️ สถาปัตยกรรมระบบ (System Architecture)

### ภาพรวมการทำงาน (Current Deployment Architecture)

```
┌─────────────────────────────────────────────────────────────────┐
│                      👤 User (Browser)                          │
│                   https://your-app.netlify.app                  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTPS Request
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              🌐 FRONTEND - Netlify (CDN)                        │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Quasar Framework (Vue 3 + Composition API)               │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │  📄 IndexPage.vue                                   │  │  │
│  │  │    - Task List UI (Card-based Design)              │  │  │
│  │  │    - fetchTasks() → axios.get('/tasks')            │  │  │
│  │  │    - Thai Date Formatting (พ.ศ.)                   │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │  🔧 boot/axios.js                                   │  │  │
│  │  │    baseURL: process.env.API_URL                    │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │                                                             │  │
│  │  📦 Build Output: dist/spa/                                │  │
│  │  🔐 Environment: API_URL                                   │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP Request
                             │ GET/POST/PUT/DELETE
                             │ https://xxx.onrender.com/api/tasks
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              ⚙️  BACKEND - Render.com (Web Service)             │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Express.js Server (Node.js + TypeScript)                 │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │  📝 src/server.ts (Entry Point)                     │  │  │
│  │  │    - app.listen(PORT)                               │  │  │
│  │  │    - Middleware: CORS, Helmet, Morgan               │  │  │
│  │  │    - Routes: /, /api/demo, /api/tasks               │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │  🛣️  src/routes/task.routes.ts                      │  │  │
│  │  │    GET    /api/tasks      → findMany()             │  │  │
│  │  │    GET    /api/tasks/:id  → findUnique()           │  │  │
│  │  │    POST   /api/tasks      → create()               │  │  │
│  │  │    PUT    /api/tasks/:id  → update()               │  │  │
│  │  │    DELETE /api/tasks/:id  → delete()               │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │  🗄️  src/prisma.ts (ORM Client)                     │  │  │
│  │  │    - PrismaClient with PrismaPg adapter            │  │  │
│  │  │    - Connection: DATABASE_URL                      │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │                                                             │  │
│  │  🔐 Environment Variables:                                 │  │
│  │     DATABASE_URL (Supabase Connection String)             │  │
│  │     PORT=3000                                              │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ PostgreSQL Protocol
                             │ Port 6543 (Transaction Pooler)
                             │ ?pgbouncer=true
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│              🗃️  DATABASE - Supabase (PostgreSQL)               │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  PostgreSQL Database (Cloud-hosted)                       │  │
│  │  ┌─────────────────────────────────────────────────────┐  │  │
│  │  │  📊 Table: tasks                                    │  │  │
│  │  │  ┌───────────────────────────────────────────────┐  │  │  │
│  │  │  │  id          UUID         PRIMARY KEY         │  │  │  │
│  │  │  │  title       VARCHAR(255) NOT NULL            │  │  │  │
│  │  │  │  description TEXT          NULLABLE            │  │  │  │
│  │  │  │  createdAt   TIMESTAMP     DEFAULT NOW()      │  │  │  │
│  │  │  │  updatedAt   TIMESTAMP     AUTO UPDATE        │  │  │  │
│  │  │  └───────────────────────────────────────────────┘  │  │  │
│  │  └─────────────────────────────────────────────────────┘  │  │
│  │                                                             │  │
│  │  🔧 Management Tools:                                       │  │
│  │     - Supabase Table Editor (Web UI)                       │  │
│  │     - Prisma Studio (npx prisma studio)                    │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow (การไหลของข้อมูล)

### 📖 Read Operation (ดึงข้อมูล)
```
User clicks "Reload Tasks" Button
         │
         ▼
┌────────────────────────┐
│  IndexPage.vue         │
│  fetchTasks()          │
└───────────┬────────────┘
            │
            │ axios.get('/tasks')
            ▼
┌────────────────────────┐
│  Axios Client          │
│  baseURL + '/tasks'    │
└───────────┬────────────┘
            │
            │ HTTPS Request
            │ GET https://xxx.onrender.com/api/tasks
            ▼
┌────────────────────────┐
│  Express Server        │
│  task.routes.ts        │
└───────────┬────────────┘
            │
            │ router.get('/')
            ▼
┌────────────────────────┐
│  Prisma Client         │
│  prisma.task.findMany()│
└───────────┬────────────┘
            │
            │ SQL Query
            │ SELECT * FROM tasks ORDER BY createdAt DESC
            ▼
┌────────────────────────┐
│  Supabase Database     │
│  Return rows           │
└───────────┬────────────┘
            │
            │ JSON Response
            │ { data: [...tasks] }
            ▼
┌────────────────────────┐
│  Frontend UI           │
│  Display in Cards      │
└────────────────────────┘
```

### ✍️ Create Operation (เพิ่มข้อมูล)
```
curl -X POST /api/tasks -d "{...}"
         │
         ▼
┌────────────────────────┐
│  Express Server        │
│  router.post('/')      │
└───────────┬────────────┘
            │
            │ Validate request body
            ▼
┌────────────────────────┐
│  Prisma Client         │
│  prisma.task.create()  │
└───────────┬────────────┘
            │
            │ SQL INSERT
            │ INSERT INTO tasks (id, title, description, ...)
            │ VALUES (uuid_generate_v4(), ...)
            ▼
┌────────────────────────┐
│  Supabase Database     │
│  Auto-generate UUID    │
│  Set timestamps        │
└───────────┬────────────┘
            │
            │ Return created record
            │ { data: { id: "...", title: "...", ... } }
            ▼
┌────────────────────────┐
│  Response to Client    │
│  Status: 201 Created   │
└────────────────────────┘
```

---

## 🗂️ โครงสร้างไฟล์โปรเจกต์

```
ch3.1-proxy-server-main/
│
├── 📁 backend/                       # Backend Service (Render)
│   ├── 📁 src/
│   │   ├── 📄 server.ts              # Express Entry Point
│   │   ├── 📄 prisma.ts              # Prisma Client Setup
│   │   └── 📁 routes/
│   │       └── 📄 task.routes.ts     # CRUD API Routes
│   │
│   ├── 📁 prisma/
│   │   ├── 📄 schema.prisma          # Database Schema
│   │   └── 📄 seed.js                # Seed Script (Idempotent)
│   │
│   ├── 📄 prisma.config.ts           # Prisma 7 Config
│   ├── 📄 .env                       # Local Environment
│   ├── 📄 Dockerfile                 # Container Config
│   ├── 📄 package.json               # Dependencies
│   └── 📄 tsconfig.json              # TypeScript Config
│
├── 📁 frontend/                      # Frontend App (Netlify)
│   ├── 📁 src/
│   │   ├── 📁 pages/
│   │   │   └── 📄 IndexPage.vue      # Main Task List
│   │   ├── 📁 boot/
│   │   │   └── 📄 axios.js           # HTTP Client Config
│   │   └── 📁 layouts/
│   │       └── 📄 MainLayout.vue     # App Layout
│   │
│   ├── 📁 public/
│   │   └── 📄 _redirects             # Netlify SPA Routing
│   │
│   ├── 📄 quasar.config.js           # Framework Config
│   └── 📄 package.json               # Dependencies
│
├── 📄 คู่มือการ deploy.md            # Deployment Guide
└── 📄 ARCHITECTURE.md                # This File
```

---

## 🛠️ Technology Stack

| Layer | Technology | Purpose |
|-------|-----------|---------|
| **Frontend** | Quasar v2 (Vue 3) | UI Framework |
| | Axios | HTTP Client |
| | Composition API | State Management |
| **Backend** | Express.js v5 | Web Server |
| | TypeScript | Type Safety |
| | Prisma v7 | ORM |
| | @prisma/adapter-pg | PostgreSQL Adapter |
| **Database** | PostgreSQL | Relational Database |
| | Supabase | Cloud Hosting |
| **Deployment** | Netlify | Frontend CDN |
| | Render.com | Backend Hosting |
| | GitHub | Version Control |

---

## 🔐 Environment Configuration

### Backend (Render.com)
```bash
DATABASE_URL="postgresql://postgres.xxx:pass@aws-xxx.pooler.supabase.com:6543/postgres?pgbouncer=true"
PORT=3000
NODE_ENV=production
```

### Frontend (Netlify)
```bash
API_URL="https://your-backend.onrender.com/api"
```

### Local Development
```bash
# backend/.env
DATABASE_URL="postgresql://..."
PORT=3000

# frontend/.env (optional)
API_URL="http://localhost:3000/api"
```

---

## 📡 API Endpoints Reference

| Method | Endpoint | Description | Request Body | Response |
|--------|----------|-------------|--------------|----------|
| GET | `/` | Health Check | - | `{ message, timestamp }` |
| GET | `/api/tasks` | Get all tasks | - | `{ data: [...] }` |
| GET | `/api/tasks/:id` | Get single task | - | `{ data: {...} }` |
| POST | `/api/tasks` | Create task | `{ title, description? }` | `{ data: {...} }` |
| PUT | `/api/tasks/:id` | Update task | `{ title?, description? }` | `{ data: {...} }` |
| DELETE | `/api/tasks/:id` | Delete task | - | `{ message }` |

---

## 🚀 CI/CD Pipeline

```
Developer commits code
         │
         ▼
    git push origin main
         │
         ▼
┌────────────────────┐
│  GitHub Repository │
└────────┬───────────┘
         │
         ├─────────────────────┬─────────────────────┐
         │                     │                     │
         ▼                     ▼                     ▼
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  Render Webhook  │  │ Netlify Webhook  │  │  GitHub Actions  │
│                  │  │                  │  │   (Optional)     │
└────────┬─────────┘  └────────┬─────────┘  └──────────────────┘
         │                     │
         │ Auto Deploy         │ Auto Deploy
         ▼                     ▼
┌──────────────────┐  ┌──────────────────┐
│  Build Backend   │  │  Build Frontend  │
│  1. npm install  │  │  1. npm install  │
│  2. prisma gen   │  │  2. quasar build │
│  3. npm build    │  │  3. Deploy CDN   │
│  4. npm start    │  │                  │
└────────┬─────────┘  └────────┬─────────┘
         │                     │
         ▼                     ▼
    Backend Live          Frontend Live
```

---

## 💡 Key Features

### 🌱 Database Seeding
- **File**: `backend/prisma/seed.js`
- **Idempotent**: Checks existing data before insert
- **Initial Data**: 3 sample tasks
- **Command**: `npx prisma db seed`

### 🎨 Frontend UI
- **Card-based Design**: Modern, responsive
- **Hover Effects**: Smooth animations
- **Thai Dates**: Buddhist calendar (พ.ศ.)
- **Real-time**: Refresh button
- **Error Handling**: User-friendly messages

### 🔒 Security
- **CORS**: Configured for frontend origin
- **Helmet**: Security headers
- **Environment Variables**: No secrets in code
- **Connection Pooling**: Optimized DB connections

---

*สร้างโดย: Full Stack Development Lab*  
*อัปเดตล่าสุด: 10 กุมภาพันธ์ 2569*
